apiVersion: v1
kind: Service
metadata:
  name: incident-service
  labels: 
    app: incident-app
spec:
  type: NodePort
  selector:
    app: incident-app
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30000
    name: http